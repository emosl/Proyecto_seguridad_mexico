// import React from 'react';
// import { useCanAccess } from '@react-admin/ra-rbac';

// const MyComponent = () => {
//   const canAccess = useCanAccess();

//   // Use canAccess to check if the user has access to a specific resource or action
//   const canAccessResource = canAccess('resource', 'read');
//   const canAccessAction = canAccess('action', 'delete');

//   return (
//     <div>
//       <p>User can access resource: {canAccessResource ? 'Yes' : 'No'}</p>
//       <p>User can access action: {canAccessAction ? 'Yes' : 'No'}</p>
//     </div>
//   );
// };

// export default MyComponent;
